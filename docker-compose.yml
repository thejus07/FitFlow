services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: pulse_gym
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ["55432:5432"]
    volumes: ["postgres_data:/var/lib/postgresql/data"]
volumes:
  postgres_data:
